package fundamentos.controles

fun main() {
    var contador: Int = 1

    /**
     * Controle da quantidade de repetições, neste caso o ideal é utilizar o laço for
     */
    while (contador <= 10) {
        println(contador)
        contador++
    }
}
